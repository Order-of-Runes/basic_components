name: basic_components
description: "A collection of basic components"
version: 0.0.6
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  better_components:
    git:
      url: https://github.com/Order-of-Runes/better_components.git
      ref: 0.0.3
  flutter:
    sdk: flutter
  utils:
    git:
      url: https://github.com/Order-of-Runes/utils.git
      ref: 0.0.5

dev_dependencies:
  very_good_analysis: ^7.0.0